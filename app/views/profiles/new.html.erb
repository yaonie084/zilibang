<div class="content-box">
	<div class="content-box-header">
		<h3>填写资料</h3>
	</div>

	<div class="content-box-content">
		<%= semantic_form_for(@profile, :url => profiles_path(), :html => { :multipart => true }) do |f| %>
			<%= render :partial => 'form', :object => f %>
		<% end %>
	</div>
</div>