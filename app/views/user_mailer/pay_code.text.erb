您好，<%=@user.email%>:
  您在自强帮（http://localhost:3000）发布的"<%= @post.title %>"任务已经付清款数。
  下面是接受您任务的用户的提款代码。请在完成您的工作后，将代码发给对应用户名的用户。
  用户邮箱与代码表如下：
    <%  @post.comments.each do |comment| %>
      <%  if comment.code != nil %>
       <%=  "#{comment.user.email}: #{comment.code}\n" %>
      <%  end %>
  <% end %>
  请妥善保管提管密码，务必等待任务完成后，再发放给员工。自强帮是凭借此提款密码来确认工人是否完工的，对于没有输入提款密码的中标工人，自强帮将退还这部分款项。
  任务地址：<%= @url %>